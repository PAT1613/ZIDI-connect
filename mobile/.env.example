# API base URL for the Django backend. Must include /api/v1.
# Use your dev machine's LAN IP (not localhost) so a physical device/emulator can reach it.
EXPO_PUBLIC_API_BASE_URL=http://192.168.0.194:8000/api/v1
